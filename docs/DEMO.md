# Demo Steps

1. Create `.env` in `infra/` with required variables.
2. `cd infra && docker compose --env-file .env up --build`
3. Visit `http://localhost:3000` and place orders.
4. Show WS trades and relayer logs.
5. (Optional) Show settlement tx hash on explorer.


